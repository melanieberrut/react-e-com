import React from 'react';
import SignOutButton from '../components/SignOut';

const MyAccount = ({ history }) => (
  <React.Fragment>
    <h1>My Account page (WIP)</h1>
    <SignOutButton history={history} />
  </React.Fragment>
);

export default MyAccount;
